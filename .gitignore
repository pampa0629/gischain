# macOS
.DS_Store

# Python
*.pyc
__pycache__/

# 密钥配置（本地保留，不入库）
config.ini

# 数据文件（体积大，按需本地准备）
data/yanshi.udbx
data/terrain.tif
data/规划用地.*
data/城区范围.*

# 运行时的中间产物和最终结果
data/temp/
data/output/

# 历史遗留：项目内误建的 venv（名为 gischain，与包目录同名，建议删除）
gischain/bin/
gischain/lib/
gischain/include/
gischain/pyvenv.cfg

# WorkBuddy 项目记忆（阿链的跨会话记忆，勿提交）
.workbuddy/

# 临时文件
*.cpg
